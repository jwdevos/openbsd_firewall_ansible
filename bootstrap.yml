---
- hosts: all
  gather_facts: true
  
  tasks:
  - name: set /etc/installurl
    raw: echo "https://fastly.cdn.openbsd.org/pub/OpenBSD" > /etc/installurl
    ignore_errors: true
    changed_when: false
